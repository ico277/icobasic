#include <iostream>
#include <fstream>
#include <vector>

#include "b2a.h"

using std::vector;
using std::pair;
using std::ifstream;
using std::string;

vector<b2a_instruction> b2a_parse_tokens(vector<pair<b2a_token, string>>) {
    vector<b2a_instruction> vec;

    return vec;
}
